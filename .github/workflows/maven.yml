# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
    - name: SendGrid Mail Action
     # You may pin to the exact commit or the version.
     # uses: mmichailidis/sendgrid-mail-action@8d40a96373d5717855a7d4c06628c9cb2dcdc8d7
      uses: mmichailidis/sendgrid-mail-action@v1.1
      with:
        sendgrid-token: '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' 
    # The emails ( can be only one ) that the email will go
        mail: 'narendra.kumar02@nagarro.com'    # The email that will be shown as sender
        from: 'sendgridmailaction@github.com'
    # The subject of the email
        subject: 'Success'
    # Defines if it should be one email with multiple address or multiple emails with a single address
      
